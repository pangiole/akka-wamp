package examples


object ScalaStreamsApp {
  def main(args: Array[String]): Unit = {
    
    /*
    val client = Client("crossbar", "hello.realm")
    val source: Source[Event, _] = client.subscribe("myapp.topic")
    source.runForeach { event => 
      println(event)
    }
    
    val s: Source[Invocation, _] = client.register("myapp.procedure")
    s.runForeach { (invocation) =>
      println(invocation)
    }*/
  }
}