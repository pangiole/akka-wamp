akka {
  loggers = ["akka.event.Logging$DefaultLogger"]
  loglevel = off
  test {
    timefactor = 5
  }
  wamp {
    router {
      transport {
        default {
          port = 0        
       }
      }
    }
  }
}